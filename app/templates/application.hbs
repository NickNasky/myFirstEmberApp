{{tool-bar
  messages=messages
  model=model
  someSelected=someSelected
  allSelected=allSelected
  noneSelected=noneSelected
  allDeleted=allDeleted
  createMessage=createMessage
  unreadMessages=unreadMessages
  toggleMessage=(action "toggleMessage")
  markAsRead=(action "markAsRead")
  markAsUnread=(action "markAsUnread")
  deleteSelected=(action "deleteSelected")
  addLabel=(action "addLabel")
  removeLabel=(action "removeLabel")
  toggleChecked=(action "toggleChecked")}}

{{create-message
  createMessage=createMessage}}

{{#each model as |messages|}}
  {{inbox-messages
  messages=messages
  model=model
  someSelected=someSelected
  allSelected=allSelected
  noneSelected=noneSelected
  allDeleted=allDeleted
  createMessage=createMessage
  unreadMessages=unreadMessages
  selection=(action "selectMessage")
  makeStarred=(action "toggleStarred")}}
{{/each}}
  <br>
{{outlet}}
